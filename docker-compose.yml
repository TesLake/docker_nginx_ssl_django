version: "3.8"

services:
  nginx:
    restart: always
    build:
      context: ./nginx
    ports:
      - 80:80
      - 443:443
    depends_on:
      - api
      - certbot
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
  api:
    build:
      context: .
  certbot:
    image: certbot/certbot
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
